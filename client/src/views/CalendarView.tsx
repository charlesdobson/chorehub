import { ReactElement } from 'react';
import { ViewWrapper } from 'components';

const CalendarView = (): ReactElement => {
  return <ViewWrapper>CalendarView</ViewWrapper>;
};

export default CalendarView;
